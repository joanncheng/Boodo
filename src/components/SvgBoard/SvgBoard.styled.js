import styled from 'styled-components';

export const SVGCanvas = styled.svg`
  width: 100%;
  height: 100%;
  overflow: hidden;
`;

export const TextArea = styled.textarea`
  font-family: 'Gochi Hand', 'Virgil', 'Klee One', 'Noto Sans TC', Arial,
    sans-serif;
  font-size: 24px;
  letter-spacing: 1.5px;
  width: 50%;
  height: fit-content;
  resize: none;
  margin: 0;
  padding: 0;
  border: 0;
  outline: none;
  overflow: hidden;
  background: transparent;
`;
